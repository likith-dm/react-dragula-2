import React from "react";
import Palette from "../Palette/Palette";

const AppPalette: React.FC = () => {
  return (
    <div className="palette-dom">
      <Palette.Index />
    </div>
  );
};

export default AppPalette;
